{
    "name": "osiris",
    "version": "1.0.0",
    "main": "src/main.js",
    "devDependencies": {
        "electron": "^9.1.0",
        "electron-build-env": "^0.2.0",
        "parser": "file:parser/",
        "neon-cli": "^0.4.0"
    },
    "scripts": {
        "build": "electron-build-env neon build parser --release",
        "start": "electron ."
    }
}