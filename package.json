{
    "name": "osiris",
    "version": "1.0.0",
    "main": "src/main.js",
    "devDependencies": {
        "electron": "^18.3.7",
        "electron-build-env": "^0.2.0",
        "neon-cli": "^0.4.0",
        "parser": "file:parser/",
        "yarn-install": "^1.0.0"
    },
    "scripts": {
        "build": "electron-build-env neon build parser --release",
        "install": "yarn build",
        "start": "electron ."
    },
    "dependencies": {
        "jquery": "^3.5.1",
        "jstree": "^3.3.10",
        "npm": "^6.14.6"
    }
}
